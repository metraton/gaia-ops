# Gaia-Ops Environment Configuration
# Copy this file to .env and adjust values for your environment

# ============================================================================
# Project Root Paths
# ============================================================================

# Primary project root (where CLAUDE.md lives)
PROJECT_ROOT=$(pwd)

# Alternative: Set explicitly if running from different directory
# PROJECT_ROOT=/path/to/your/project

# ============================================================================
# Documentation & System Paths
# ============================================================================

# Gaia documentation path (for agent references)
GAIA_DOCS_PATH=$PROJECT_ROOT/.claude

# Tools path
TOOLS_PATH=$PROJECT_ROOT/.claude/tools

# Config path
CONFIG_PATH=$PROJECT_ROOT/.claude/config

# ============================================================================
# Infrastructure & Cloud Paths
# ============================================================================

# GitOps repository (operations)
GITOPS_PATH=$PROJECT_ROOT/../ops

# Terraform infrastructure
TERRAFORM_PATH=$PROJECT_ROOT/../ops/terraform

# Terraform modules
TERRAFORM_MODULES_PATH=$PROJECT_ROOT/../ops/terraform/modules

# Terraform environments
TERRAFORM_ENVIRONMENTS_PATH=$PROJECT_ROOT/../ops/terraform/environments

# Application services
APP_SERVICES_PATH=$PROJECT_ROOT/../app-services

# Shared infrastructure (for multi-project setups)
SHARED_INFRA_PATH=$PROJECT_ROOT/../shared-infra

# ============================================================================
# Shared & Multi-Project Configuration
# ============================================================================

# Shared Claude configuration (used by multiple projects)
CLAUDE_SHARED_PATH=$PROJECT_ROOT/.claude-shared

# Shared settings file (applied to all projects)
CLAUDE_SHARED_SETTINGS=$PROJECT_ROOT/.claude-shared/settings.json

# Project-specific settings (overrides shared settings)
CLAUDE_PROJECT_SETTINGS=$PROJECT_ROOT/.claude/settings.json

# ============================================================================
# Permissions & Security
# ============================================================================

# Shared security rules (deny > ask > allow priority)
SECURITY_DENY_LIST=$PROJECT_ROOT/.claude-shared/settings.json

# Project-specific overrides
SECURITY_PROJECT_OVERRIDES=$PROJECT_ROOT/.claude/settings.json

# ============================================================================
# Cloud Provider Configuration
# ============================================================================

# GCP Configuration
GCP_PROJECT_ID=your-gcp-project-id
GCP_CREDENTIALS_PATH=$HOME/.config/gcloud/application-default-credentials.json
GCP_REGION=us-central1

# AWS Configuration
AWS_PROFILE=default
AWS_REGION=us-west-2

# ============================================================================
# Development & Testing
# ============================================================================

# Test fixtures directory
TEST_FIXTURES_PATH=$PROJECT_ROOT/tests/fixtures

# Test output directory
TEST_OUTPUT_PATH=$PROJECT_ROOT/tests/output

# Python path for tools/agents
PYTHONPATH=$PROJECT_ROOT/.claude/tools:$PROJECT_ROOT/tools

# ============================================================================
# Logging & Auditing
# ============================================================================

# Log directory
LOG_PATH=$PROJECT_ROOT/.claude/logs

# Audit log level (DEBUG, INFO, WARNING, ERROR)
AUDIT_LOG_LEVEL=INFO

# Audit log retention (days)
AUDIT_LOG_RETENTION=90

# ============================================================================
# Usage Notes
# ============================================================================

# 1. Copy this file:
#    cp .env.example .env
#
# 2. Customize for your environment:
#    - Update PROJECT_ROOT if needed
#    - Set cloud provider credentials
#    - Adjust paths for your infrastructure layout
#
# 3. Source in bash scripts:
#    set -a; source .env; set +a
#
# 4. Or load in Python:
#    from dotenv import load_dotenv
#    load_dotenv()
#    project_root = os.getenv("PROJECT_ROOT")
#
# 5. DO NOT commit .env to version control
#    Add to .gitignore:
#    echo ".env" >> .gitignore
#    echo ".env.local" >> .gitignore
