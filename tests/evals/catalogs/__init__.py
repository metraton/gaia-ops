"""YAML catalog files for the eval framework.

Each catalog (e.g. ``context_consumption.yaml``) declares a batch of
cases that :mod:`tests.evals.catalog` loads into ``CaseModel`` objects.
This package is empty at the Python level; it exists to keep the YAML
directory importable and discoverable alongside the rest of the
``tests.evals`` tree.
"""
